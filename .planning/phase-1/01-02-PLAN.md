---
phase: 1
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/layout.tsx, src/components/navbar.tsx, src/components/footer.tsx]
autonomous: true
requirements: ["CORE-01"]
must_haves:
  truths:
    - "A navegação (Navbar) aparece em todas as rotas"
    - "Os links da Navbar redirecionam para as seções corretas"
  artifacts:
    - path: "src/components/navbar.tsx"
      provides: "Main navigation"
    - path: "src/app/layout.tsx"
      provides: "App wrapper"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/navbar.tsx"
      via: "import Navbar"
---

<objective>
Construir o layout responsivo base com navegação principal (Sobre, Projetos, Contato).

Purpose: Garantir que a navegação do site está funcional desde o início.
Output: Componente Navbar, Componente Footer, Layout atualizado.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-1/01-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install shadcn UI components</name>
  <files>components.json</files>
  <action>Run `npx -y shadcn@latest add button sheet navigation-menu` to install the necessary base components.</action>
  <verify>Check if `src/components/ui/button.tsx` exists.</verify>
  <acceptance_criteria>
    - Button component is installed
  </acceptance_criteria>
  <done>UI components ready</done>
</task>

<task type="auto">
  <name>Task 2: Build Navbar Component</name>
  <files>src/components/navbar.tsx</files>
  <action>Create a responsive Navbar component exporting a default function `Navbar`. Should have a desktop navigation (using Next.js `Link` for "Sobre", "Projetos", "Contato") and a mobile navigation (using a `Sheet` or simple hamburger menu). Use `lucide-react` for any icons.</action>
  <verify>Check `src/components/navbar.tsx` exists and exports Navbar.</verify>
  <acceptance_criteria>
    - File exists and has proper Next.js links.
  </acceptance_criteria>
  <done>Navbar built</done>
</task>

<task type="auto">
  <name>Task 3: Apply Layout Shell</name>
  <files>src/app/layout.tsx, src/components/footer.tsx</files>
  <action>Create a simple `Footer` component in `src/components/footer.tsx`. Then update `src/app/layout.tsx` to include `<Navbar />` at the top of the body, then `{children}` wrapped in a main tag, then `<Footer />`.</action>
  <verify>Check `src/app/layout.tsx` imports Navbar.</verify>
  <acceptance_criteria>
    - Layout contains Navbar and Footer
  </acceptance_criteria>
  <done>Layout applied</done>
</task>
</tasks>

<verification>
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- Navigation is responsive
</success_criteria>

<output>
After completion, create `.planning/phase-1/01-02-SUMMARY.md`
</output>
