---
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.mjs, tailwind.config.ts, src/app/layout.tsx, src/app/globals.css, components.json]
autonomous: true
requirements: ["CORE-01"]
must_haves:
  truths:
    - "Next.js framework initializes correctly"
    - "TailwindCSS and shadcn/ui are properly configured"
  artifacts:
    - path: "package.json"
      provides: "Dependencies"
    - path: "components.json"
      provides: "Shadcn configuration"
  key_links: []
---

<objective>
Setup Next.js, Tailwind CSS, and shadcn/ui.

Purpose: Estabelecer a base técnica (framework e design system) do portfólio.
Output: Repositório com Next.js configurado e rodando.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-1/01-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap Next.js App Router</name>
  <files>package.json, next.config.mjs, src/app/layout.tsx, src/app/globals.css</files>
  <action>Run `npx -y create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` in non-interactive mode. (Since we are in the root `.`, we might need to force it or create it in a temp dir and move it. Better: use `npx create-next-app@latest . --yes --tailwind --ts --eslint --app --src-dir --import-alias "@/*"`). Note: se o diretório não estiver vazio, criar em `./portfolio` e mover os arquivos para a raiz, removendo o `./portfolio`.</action>
  <verify>Check if `package.json` exists and contains next.</verify>
  <acceptance_criteria>
    - `package.json` contains "next"
  </acceptance_criteria>
  <done>Next.js setup complete</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css</files>
  <action>Run `npx -y shadcn@latest init -d`. It should use the default configuration for the new-york style and neutral color slate. Then update `tailwind.config.ts` and `src/app/globals.css` with the colors from UI-SPEC (Background: #ffffff/#09090b, Accent: #3b82f6).</action>
  <verify>Check if `components.json` exists.</verify>
  <acceptance_criteria>
    - `components.json` is present
    - `globals.css` contains CSS variables for shadcn
  </acceptance_criteria>
  <done>shadcn/ui configured</done>
</task>
</tasks>

<verification>
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- Base project compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phase-1/01-01-SUMMARY.md`
</output>
