---
status: planned
phase: 6
gap_closure: true
---

# Plan 03: Gap Closure for Syntax Error and Compilation

<task>
<objective>
Investigar e corrigir o erro "Unexpected token section. Expected jsx identifier" e validar a compilação geral do projeto para liberar o teste das animações.
</objective>

<read_first>
- src/app/page.tsx
- src/components/reveal.tsx
- src/components/projects-section.tsx
- src/components/about-section.tsx
- package.json
</read_first>

<action>
1. Investigar a raiz do erro "Unexpected token section" em arquivos que usam JSX (`src/app/page.tsx`, `src/components/reveal.tsx`, `src/components/projects-section.tsx`, `src/components/about-section.tsx`). Pode ser que o uso de `section` não foi bem parseado pelo compilador do Next.js.
2. Corrigir o erro (ex: transformar `section` problemático em `div` provisório ou corrigir o `motion.div` no `reveal.tsx`).
3. Verificar a correta instalação e importação do pacote `framer-motion` em todos os componentes Client (aqueles com `"use client"`).
4. Executar um build de teste (`npm run build`) para atestar que o problema foi completamente solucionado.
</action>

<acceptance_criteria>
- A aplicação compila sem erros (a mensagem "failed to compile" ou "Unexpected token section" desaparece).
</acceptance_criteria>

<verify>
- `npm run build` retorna exit code 0.
</verify>

<must_haves>
<truths>
- As animações documentadas originalmente (`framer-motion`) devem continuar presentes ou serem reimplementadas sem falhas de sintaxe.
- A página compila e exibe o Hero com fade-in e os projetos com fade-up.
</truths>
<prohibitions>
- Não reverter as animações ou remover os pacotes recém-adicionados sem providenciar um conserto.
</prohibitions>
</must_haves>
</task>
