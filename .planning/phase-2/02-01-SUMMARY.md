# Plan Summary

**Phase:** 2
**Plan:** 01
**Status:** Complete

**Tasks completed:**
1. Instalação da biblioteca `next-themes` para suporte a dark mode com Next.js App Router.
2. Criação do componente cliente `ThemeProvider` (`src/components/theme-provider.tsx`).
3. Criação do botão `ThemeToggle` (`src/components/theme-toggle.tsx`) utilizando os ícones `Sun` e `Moon` da biblioteca `lucide-react`.
4. Atualização do `src/app/layout.tsx` para embrulhar o conteúdo principal no `ThemeProvider`.
5. Atualização da `src/components/navbar.tsx` para incluir o botão de alterar tema tanto na visão desktop quanto mobile.

**Artifacts created:**
- `src/components/theme-provider.tsx`
- `src/components/theme-toggle.tsx`
- `src/app/layout.tsx` (modificado)
- `src/components/navbar.tsx` (modificado)
